return {
  "snacks.nvim",
  opts = {
    scroll = { enabled = false }, -- disable animated scrolling
    picker = { sources = { explorer = { layout = { layout = { position = "right" } } } } }, -- position explorer on the right
  },
}
